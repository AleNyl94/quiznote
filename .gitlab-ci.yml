image: node:20

stages:
  - build
  - test
  - deploy

build-job:
  stage: build
  script:
    - cd server
    - npm ci --legacy-peer-deps
  artifacts:
    paths:
      - server/node_modules/

test-job:
  stage: test
  script:
    - cd server
    - npm run eslint
    - npm test
    - npm test
    - cd ..
    - cd client
    - npm ci
    - npm run eslint
    - npm test

  dependencies:
    - build-job

deploy-job:
  stage: deploy
  script:
    - echo "Deploying to production server..."
  when: manual
  only:
    - main